/// ingestion-types.mo
/// All canonical types have been consolidated into types/common.mo.
/// This module is kept for backwards-compatible import paths but re-exports
/// nothing — consumers should import from types/common.mo directly.
module {};
